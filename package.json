{
  "name": "baptiste-fort-conversational-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs",
    "build:pages": "node scripts/build-pages.mjs",
    "worker:dev": "wrangler dev",
    "worker:deploy": "wrangler deploy",
    "check": "node --check app.js && node --check server.mjs && node --check persona.mjs && node --check scope-guard.mjs && node --check worker.mjs && node --check scripts/build-pages.mjs"
  },
  "dependencies": {
    "openai": "^6.46.0"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
